import 'package:{package}/common/styles/color_style.dart';
import 'package:{package}/common/widgets/scaffold/scaffold.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';

import '{name}_logic.dart';

class {PageClass} extends StatefulWidget {
  const {PageClass}({super.key});

  @override
  State<{PageClass}> createState() => _{PageClass}State();
}

class _{PageClass}State extends State<{PageClass}> with {LogicClass}Mixin {
  @override
  Widget build(BuildContext context) {
    return {Prefix}Scaffold(
      backgroundColor: {Prefix}ColorStyles.surface,
      body: Container(),
    );
  }
}